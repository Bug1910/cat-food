# 執行狀態 — Cat_Food（貓罐頭促銷網站）

## 這是什麼

一個賣「純肉貓罐頭」的產品介紹網頁，用 Figma Make（一個能把設計稿直接生成網頁程式碼的 AI 工具）產出，
框架是 React + Vite（React 負責畫面元件化、Vite 是幫你把程式碼打包成瀏覽器看得懂的東西的建置工具）。

頁面內容：主視覺、限時優惠彈窗、六種口味產品清單（含營養成分長條圖）、品質保證、常見問題。

## 掃描結果

- 沒有找到寫死的絕對路徑（`C:\Users\...` 這類）
- 沒有找到失效外部連結
- 沒有連到已下線的 `specialtylife.com.tw`
- 圖片走 Unsplash（一個免費圖庫網站）CDN 連結，目前都能正常載入

## 修了什麼

沒有需要修的東西，路徑跟連結原本就是乾淨的。

## 怎麼測

1. 進到專案資料夾，開終端機
2. 跑 `npm.cmd install`（Windows 上 PowerShell 執行原則會擋 `npm`，要打 `npm.cmd`）
3. 跑 `npm.cmd run dev -- --port 5173`
4. 瀏覽器打開 `http://localhost:5173/`

## 實測結果：PASS

- 用 Playwright（自動化瀏覽器測試工具）實際開啟並截圖，存在 `screenshot.png`
- 瀏覽器 console（開發者工具的錯誤訊息面板）沒有紅色錯誤，只有一條無關痛癢的提醒（某個下拉選單元件建議父層加 `position: relative`，不影響顯示）
- 全頁截圖確認：主視覺、優惠彈窗、六個口味卡片、品質保證區塊、FAQ 手風琴選單全部正常顯示

## 還有什麼問題

沒有。這個專案可以直接跑。

## 補充：不要升級套件

`npm install` 過程 npm 會提示有安全性漏洞（vulnerabilities）跟一些套件版本較舊，
**依照指示沒有動任何 package.json 裡的版本號**，這些警告不影響網頁能不能正常開。
